{
  "name": "callgrinder",
  "version": "1.0.3",
  "description": "Profile a command with Valgrind callgrind and report per-category and per-routine CEst costs.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "callgrinder": "src/cli.js"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check": "node --check src/main.js && node --check src/cli.js",
    "test": "node --test"
  }
}
